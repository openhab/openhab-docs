name: Linting

on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'

jobs:
  markdownlint:
    steps:
      - name: Run markdownlint (mdl)
        uses: bewuethr/mdl-action@v1.0.12
        with:
          style-file: .github/markdownStyle.rb
